test_suite_config:
  files:
    - path: ${TORCH_DEVICE_ROOT}/tests/tensor/test_resize.py
      # Uses @instantiate_parametrized_tests — pytest collects the test classes
      # directly without run_test.sh injecting instantiate_device_type_tests.
      unlisted_test_mode: mandatory_success
