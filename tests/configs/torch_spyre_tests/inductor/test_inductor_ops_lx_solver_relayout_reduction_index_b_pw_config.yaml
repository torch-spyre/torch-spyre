# Solver-relayout sweep shard: mirrors test_inductor_ops_lx_reduction_index_b_config.yaml but runs
# Split by wrap suffix so each shard fits the 60 minute job budget: the
# CP-SAT co-optimizing configuration compiles roughly 2.5x slower per test
# than the greedy LX shards this file mirrors. The split keys on the
# _lx_planning_{pointwise,reduction} METHOD suffix: the OOT runner strips
# the ClassName:: prefix from `names:` entries and matches bare method
# names, so a class-name split does not select anything.
# tests/inductor/test_inductor_ops_lx_solver_relayout.py, the LX-planning
# wrap re-wrapped with layout_solver=cpsat, co_optimizing_lx_planning=True
# and lx_solver_relayout=True. The xfail floor is inherited from the
# LX-planning shard; deltas found by this sweep are appended per shard.
test_suite_config:
  labels: [regression, trunk]
  files:
    - path: ${TORCH_DEVICE_ROOT}/tests/inductor/test_inductor_ops_lx_solver_relayout.py
      unlisted_test_mode: skip
      tests:
        # Known LX-planning failures -- run and expected to fail. Non-strict
        # xfail: an unexpected pass (XPASS) does not fail CI. Entries failing
        # in both wraps collapse to (pointwise|reduction).
        - names:
            - SolverRelayout.*::test_aminmax_keepdim0_aminmax_2d_dim_0_lx_planning_pointwise
            - SolverRelayout.*::test_mean_keepdim1_mean_fp16_2d_dim_1_lx_planning_pointwise
            - SolverRelayout.*::test_mean_keepdim1_mean_fp16_2d_dim_0_lx_planning_pointwise
          mode: xfail
        - names:
            - SolverRelayout.*::test_aminmax_keepdim.*_lx_planning_pointwise
            - SolverRelayout.*::test_sum_keepdim0.*_lx_planning_pointwise
            - SolverRelayout.*::test_sum_keepdim1.*_lx_planning_pointwise
            - SolverRelayout.*::test_mean_keepdim0.*_lx_planning_pointwise
            - SolverRelayout.*::test_mean_keepdim1.*_lx_planning_pointwise
            - SolverRelayout.*::test_vector_norm_keepdim.*_lx_planning_pointwise
            - SolverRelayout.*::test_matrix_norm_keepdim.*_lx_planning_pointwise
            - SolverRelayout.*::test_linalg_norm_keepdim.*_lx_planning_pointwise
            - SolverRelayout.*::test_argmin_keepdim0.*_lx_planning_pointwise
            - SolverRelayout.*::test_min_tuple_output_keepdim0.*_lx_planning_pointwise
            - SolverRelayout.*::test_logsumexp_keepdim0_known_xfail.*_lx_planning_pointwise
            - SolverRelayout.*::test_mean_3d_dim0_keepdim.*_lx_planning_pointwise
            - SolverRelayout.*::test_index_copy_.*_lx_planning_pointwise
            # Regression guard for LX producer/consumer core->slice agreement
            # (#3374, #3387). Asserts both values and that the buffer stays in LX.
            - SolverRelayout.*::test_shared_input_two_reductions.*_lx_planning_pointwise
          mode: mandatory_success
