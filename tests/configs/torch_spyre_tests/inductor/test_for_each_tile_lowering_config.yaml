test_suite_config:
  labels: [unit, regression, trunk]
  files:
    - path: ${TORCH_DEVICE_ROOT}/tests/inductor/test_for_each_tile_lowering.py
      unlisted_test_mode: mandatory_success
    - path: ${TORCH_DEVICE_ROOT}/tests/inductor/test_for_each_tile_trip_ranges.py
      unlisted_test_mode: mandatory_success
