test_suite_config:
  labels: [unit, regression, integration, trunk]
  files:
    - path: ${TORCH_DEVICE_ROOT}/tests/inductor/test_input_staging_division.py
      unlisted_test_mode: mandatory_success
